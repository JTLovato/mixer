import React from 'react';
import Data from '../Data'

function HavedRecipeCards(props) {

    let havedIngredients = props.selectedIngredients
    
    let now = () => {
    console.log(havedIngredients)
    console.log(Data)
    }

    now()

    return (
        <div>
buts
        </div>
    )
}

export default HavedRecipeCards;
